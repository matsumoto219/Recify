# Recify v1.1.0 Release Notes

Status: draft
Audience: development / release management
Last updated: 2026-07-12

## 概要

v1.1.0 は大型機能追加ではなく、OCR/AI品質改善、Amount Engineの安全性強化、Service Layer境界整理、レビュー理由から修正箇所へ辿るUX改善、セキュリティ回帰防止を中心にした品質改善リリース。

特に以下を重視した。

- OCR/AIの失敗時に安全に review_needed / fallback へ倒す契約をspecで固定
- review reason の許可コードを同期し、未知コードを保存・表示・判定へ混入させない
- Amount Engineの purchase_total / final_payment_total / tax / adjustment / payment 境界を強化
- 税抜/税込/非課税/複数税率/支払調整が混在する実レシート系ケースの計算精度を改善
- Service Layerの直接依存、provider gate、Usage制限、国コード、sanitizerを整理
- クライアント改ざん前提で item-level review state の保護を強化
- UI上のレビュー確認リンク、画像プレビュー、メール表示、コピー導線を改善

## OCR改善

- `Ocr::ResponseParser` の購入日時fallbackを改善
  - Azure structured date/time がない場合、近接する日付行と時刻行を結合
  - 曜日や軽い空白混入を低リスク範囲で吸収
  - 遠い時刻行や過剰推測は対象外
- OCR parserで支払時割引を adjustment candidate として抽出
  - 支払時割引、決済割引、キャッシュレス割引など明確なsigned amountのみ対象
  - 支払方法、お預かり、お釣りは除外
- OCR parserでポイント利用を payment adjustment candidate として抽出
  - ポイント利用、ポイント支払、point usageを候補化
  - ポイント付与、獲得予定ポイント、保有ポイント、利用可能ポイントは除外
- 支払方法誤認を抑制
  - お預かり、お釣り、change、ポイント、クーポン、支払時割引、キャッシュレス還元を支払方法として拾わない
  - クレジット、現金、電子マネー、QR決済など明確な支払方法は維持
- OCRの同じ金額根拠が明細・調整・支払・税詳細へ二重保存される問題を防止
  - `レジ袋中1枚` の `1枚` など数量表現を金額として扱わない
  - 袋商品と明示的な袋代を分離し、`袋代 10円` / `レジ袋代 10円` は従来どおり調整として保持
  - 商品値引、レシートクーポン、ポイント利用、キャッシュレス還元、ギフトカード、税詳細の反映先をOCR根拠から安定して判定
- レシート解析profileへ国・地域依存パターンを寄せる整理を実施
  - 日本向けprofileに寄せるべきOCRパターンを整理
  - 店舗特化判定は削除方針
- 店舗名の大文字小文字復元を改善
  - OCR正規化で小文字化された店舗名について、AIへ渡す判断材料を補強
  - 固定値はSystemSettingsで調整可能にした
- 店舗名が写っていないレシートの誤採用を抑制
  - 帳票見出し、商品名、金額行を店舗名候補から除外
  - 安全な店舗名候補がない場合は `store_name=nil` として保存
  - `store_name_missing` を確認対象として残す
  - 再解析時に過去の誤った店舗名が残らないように修正
- 購入日時が取得できない場合の確認契約を強化
  - AI応答に依存せず、サーバー側で `purchased_at_missing` を補完
  - OCR-only / AI fallback / AI success の各経路で日時欠損を確認対象として扱う
- 税率表記を小計金額として誤検知しないよう改善
  - `税抜10%` / `税込8%` などの税率表記から `10` / `8` を subtotal として採用しない
  - 本物の小計行は従来どおり抽出
- OCR polling通信の停止条件を強化
  - submitだけでなくpolling GETにも明示timeoutを適用
  - 画像取得、submit、polling、retryを含むOCR全体の最大処理時間を超えた処理を継続しない

## AI改善

- `review_reason` 許可コードを同期
  - `PromptTemplate`
  - `Ai::ResponseParser`
  - `ReviewReasons`
  - docs上の定義
- AI response parserで未知の review reason を安全に除外
- adjustment系 review reason も同じ許可コードルールで扱う
- AI deterministic guardを強化
  - name completion OFF時はOCR item名を勝手に変更しない
  - item index / adjustment source line / source text / amount evidence の検証を追加
  - blocking review reason があるのに `needs_review=false` の場合の補正を固定
- `Usage::LimitExceeded` をAI fallbackで握りつぶさない
  - quota超過を provider failure / fallback failure と混同しない
- Provider gateをfail-closedへ寄せた
  - 設定取得失敗時にOCR/AI provider callが有効側へ倒れないようにした
- AI / OCR のtimeout・retry・pollingを管理画面から安全に調整可能にした
  - 設定は解析開始時に固定し、実行中の解析へ途中反映しない
  - retry増加時も利用上限と全体処理時間を超えないよう制約
  - provider呼び出し中にも残り最大処理時間を適用し、長時間応答でworkerを占有し続けない
- 国コード契約を整理
  - 内部国コードは `JPN` / `USA` など3文字固定
  - `JP` aliasは廃止
  - `JPN`のみ正式対応、`USA`はunsupportedのまま

## Amount Engine改善

### 安全契約の固定

- `purchase_total` と `final_payment_total` の契約をspecで固定
  - `resolved[:total]` は購入合計
  - `computed[:final_payment_total]` は支払照合用
  - `Receipt.total_amount` へ保存するのは purchase total
- all rejected candidate contractを追加
  - `safe_to_auto_complete: false`
  - `selected_candidate_status: "rejected"`
  - `amount_engine.no_safe_candidate: true`
  - review draftとしてresolvedは残すが自動完了しない
- positive overpayment suppress条件を厳格化
  - analysis context
  - positive delta
  - single payment
  - cash / tendered / received / お預かり系に強く一致
  - exact final payment lineが併存しない
  - 非現金、複数支払、manual/edit_saveでは抑制しない
- `SystemSettings` 取得失敗時の税抜補正fallbackをfail-closedへ変更
  - `UnknownKeyError` / `ValidationError` 時はdisabled相当
- 手動作成・編集保存の数値入力を厳格化
  - 指数表記、文字混在、負数、通貨記号などを数値へ黙って変換しない
  - 不正な税率・割引率を未設定や0%として保存しない
- `ReceiptInput` 候補の関係検証を追加
  - 小計・税・合計が矛盾する候補を自動完了しない
  - 課税明細があるtotal-only入力で小計・税を0へ消さない
- 購入合計または実支払額が負になる過大調整を自動完了しない
- 購入金額があるのに支払行がないケースを照合済み扱いにしない
- snapshot contractを強化
  - schema_version追加
  - raw OCR text / provider raw response / endpoint / source_text / description / label-like field の漏洩防止specを追加

### ReviewPolicy境界

- `payment_amount_mismatch` は最終判定でblockingになり得ることを固定
- `price_tax_inclusion_uncertain` のwarning-only例外を厳格化
  - printed tax details netと完全整合していても、competing exact basis candidateがあればreview-required
- purchase adjustment tax allocation uncertaintyをreview-required化
  - 複数税率
  - mixed basis
  - printed tax detailsあり
  - 単一positive税率 + 非課税混在
  - purchase adjustmentのtax_rate missing
- 単一税率へ安全に一致する購入調整は税率を継承
  - 明示税率、単一税率継承、配賦不明、支払調整の対象外を区別
  - 明示0%と税率欠落を混同しない
- same-rate mixed searchが探索上限に達した場合、silent completedにしない
- Service boundary specでAmount Engine内部実装の直接呼び出しを検知

### Canonicalization / 支払調整

- point / cashless reward / gift card / coupon / payment discount の二重計上防止specを追加
- 手動入力・編集保存で明示された調整種別を、ラベル文言による推定より優先
- 同額・同種でも別source lineなら正当な別行として残す
- provenance不足時に安易にcollapseしない方針を固定
- cash tendered suppress後に tendered amount を actual payment amount と誤確定しない契約を固定
- OCR/AIの同一金額根拠は保存前に1つの所有先へ解決
  - 同じsource spanの重複だけを除外し、同額の別行は維持
  - 商品・支払・税詳細に属する金額をpurchase adjustmentへ二重計上しない
  - 購入調整の税率配賦が一意に決まらない場合は自動完了せず確認対象にする

### 編集保存の金額整合性

- 明細・調整・支払いの一部だけが送信された場合も、未送信の既存行を含む保存後の完全な集合で再計算
- 個数単位の明細はサーバー側で `単価 x 数量` を再計算
  - 古いhidden `line_total`で金額が固定される問題を修正
  - 不正文字列、0、負数の数量を1へ自動補正せず入力エラーとして扱う
  - 量り売りなどの明示line totalは従来どおり維持
- 同一nested child IDの重複送信と、同一IDの更新・削除競合を422で拒否
- 金額関連の編集時は古いreceipt金額・印字税詳細を権威候補から外し、現在の明細と調整から再計算
- 保存予定の明細・調整・支払いとAmount Engine結果を保存前に照合
  - 内部不整合がある場合は保存を止め、不正な金額のままcompletedになる経路を防止
  - 支払額不一致は保存しつつ `payment_amount_mismatch` / `review_needed`へ反映
- 購入日時・支払方法・店舗名の欠損理由と、未変更の調整review状態を編集保存後も維持
- review reasonは対応する入力項目が実際に変更された場合だけ解除
  - category変更だけで税率確認を解除しない
  - item送信だけでOCR品質確認を解除しない
- レシート編集へ楽観ロックを導入し、lock version省略や複数タブの古いフォームによる金額巻き戻しを防止
- 明細数量変更後の2回目以降のフロント再計算で、hidden line totalが旧値へ戻る問題を修正

### 大規模リファクタ

- CandidateGenerator肥大化を解消するためcandidate familyへ分割
  - ReceiptInput
  - ItemAmounts
  - PrintedTaxDetails
  - MixedTaxBasis
  - IncompleteTaxDetailsReceiptTax
- `CandidateFamilyRegistry` を追加し、候補family順序を明示
- `CandidatePipeline` を追加し、候補生成・評価入口を一本化
- `EvidenceExtractor` を追加し、evidence生成を分離
- `TaxDetailEvidence` を追加し、税詳細evidence参照を集約
- `ReviewFacts` を追加し、ReviewPolicyの判定材料を構造化
- `CandidateProfileResolver` を追加し、ResultAdapterのprofile fallbackを分離
- Amount familyの不要な委譲口を削除

### 税抜/税込/非課税混在対応

- 2019年コンビニ系の税抜/税込/非課税/複数税率/支払調整混在レシートを修正
  - `mixed_by_tax_rate_group/floor` 候補を正しく選択
  - 税抜単価を税込単価へ補正
  - payment adjustment反映後の `final_payment_total` と支払額を一致
- 印字税詳細と完全一致する税抜/税込/非課税の組み合わせで、`printed_tax_details_gross` が不適切に勝たないよう調整
- 8% / 10% の各税率グループを `なし / 税込 / 税抜 / 税込税抜混在`、非課税あり/なしで組み合わせた30通りmatrix specを追加
- 小額の10%対象で税額が0円に丸められるケースでも、印字税詳細から税率グループを保持して合計・税額を解決できるよう改善
- 国内レシートの同点候補では、明細単位より税率グループ単位の丸めを優先

## Service Layer改善

- Service Layer boundaryを強化
  - controller / job / model / helper / presenter / serviceからchild namespaceを直接呼ばない契約をspec化
  - parent facade経由に統一
- `Analysis::StoreNameCandidateClassifier` の直接参照をparent facade経由へ整理
- `ReceiptAnalysisPipeline::Result#success?` の誤用防止契約を追加
  - job enqueue判定は `next_step` を見る
- completed判定契約を強化
  - blocking review reason があればcompletedにしない
  - adjustment / payment / tax / detail flagsがstatus判定へ反映されることを固定
  - AI fallback / OCR-onlyはreview_needed
- upload / admin retry / pipeline開始時のprocessing遷移とerror clearing契約を固定
- 同じ解析run/stageの重複jobを原子的にclaimし、OCR/AI呼び出しとfinalize保存の二重実行を防止
- finalize結果と解析run終端を同じtransactionで保存
  - receiptだけcompleted、runだけfailedになる不整合を防止
- 解析失敗時のrun終端とreceipt failed更新を同じtransactionへ統一
  - 片側の保存失敗でrunとreceiptの状態が食い違う問題を防止
  - Usage limit超過時も同じ終端処理を使用
- job enqueue失敗時の状態補償を追加
  - receiptとrunを処理中のまま残さず、監査結果も失敗として記録
- OCR/AI runtime configを解析run作成時に固定
  - 設定取得に失敗したrunはproviderを呼ばず安全に停止
  - 設定変更は次に作成するrunから反映
- 外部サービスの障害・復旧状態更新を原子的に処理
  - 同時失敗時のfailure count欠落を防止
  - 状態閾値を取得できない場合は安全なcode defaultで状態記録を継続
- retry元runが対象receiptに属することを検証し、別receiptのsnapshotを再利用できないよう修正
- 相互依存するSystemSettingsの更新を直列化
  - timeout/retry、保持期間、金額上限、snapshot上限などを不正な組み合わせで保存しない
  - 明細上限とOCR/AI snapshot上限の関係を双方向に検証
- admin finalize_retry validationをstrategy-aware化
  - `ocr_only` / `fail_receipt` でAI snapshotを必須にしない
  - `ai_success` / `ai_fallback` では必要snapshotを要求
- `ReceiptAnalysisRuns.record_final_result` の二重記録疑いを確認し、契約を固定

## Security改善

- client paramsの `receipt_items_attributes[][needs_review]` / `review_reasons` を信頼しないよう修正
  - hidden field改ざんでitem-level review stateを軽くできない
  - status completed偽装につながる経路を塞いだ
- strong params / protected attributesの回帰specを追加
  - `status`
  - `amount_calculation_profile`
  - `safe_to_auto_complete`
  - `selected_candidate_status`
  - top-level `review_reasons`
  - item `raw_text`
- 他人の nested attribute id を混ぜても更新・削除できないことを確認
- Sanitizerを強化
  - snapshot / admin / audit / external detail / SecurityEvent metadataで forbidden keys を落とす
  - blocked keysを共通化
  - AnalysisRunのerror messageとOCR/AI provider関連ログからtoken、endpoint、prompt、raw responseを除去
- ActiveStorage画像URLのログ秘匿を強化
  - 画像表示・ダウンロード時の署名付きURLがRailsログに残るリスクを低減
- 保守ジョブとstorage cleanupの例外情報を秘匿
  - token、provider response、storage key、署名付きURL、ローカルパス、メールアドレスをログや失敗payloadへ残さない
  - orphan blobの確認用sampleからstorage keyと元ファイル名を除外
- SecurityEventの誤検知を抑制
  - 通常のレシート編集で送られる明細・調整・支払いのnested `id` を `parameter_tampering_attempt` として記録しない
  - top-level protected receipt attributes や nested `receipt_id` の検知は維持
- forbidden key例
  - `access_token`
  - `refresh_token`
  - `set_cookie`
  - `subscription_key`
  - `client_secret`
  - `api_key`
  - `authorization`
  - `provider_raw_response`
  - `prompt`
  - `system_prompt`
  - `user_prompt`
  - `endpoint`
  - `operation-location`
  - `operation_location`

## UI/UX改善

- review / warning reasonから該当セクションへ移動する「確認」リンクを追加
  - show画面ではedit path + anchor
  - edit画面では同一ページanchor
  - receipt-level reasonは該当セクションへ移動
  - item-level reasonは該当明細行へ移動し、明細detailを自動展開
- 画像プレビューの確認リンクで自動展開できるよう改善
- 確認リンクのブラウザ回帰specを追加
  - 同一hash再クリック
  - show -> edit anchor
  - edit内anchor
  - モバイル幅
- anchor ID管理をhelper側へ集約し、view / JSの固定文字列散在を抑制
- メールアドレス表示を幅ベースの省略へ共通化
  - PCでは表示領域があればfull email
  - 狭幅ではユーザー部・ドメイン部を崩れにくく省略
  - 管理画面テーブル・詳細画面のモバイル表示崩れを修正
- メールアドレスのコピー導線を追加
  - settings security
  - admin user detail
- プロフィールカードのメール表示を固定長短縮からCSS制御へ変更
- ダウンロードリンクのTurbo遷移扱いを抑制
  - レシート画像ダウンロード後に不要なブラウザ進捗表示が残る挙動を防止
- 解析中カードに限定した再同期を追加
  - upload直後のbroadcastを取り逃しても、OCR / AI / 完了状態へ追従
  - terminal状態で停止し、古い応答によるphase逆戻りを防止
- 数値入力フォームの操作性と安全性を改善
  - 入力途中の値を最小値・最大値へ勝手に補正せず、削除・置換・小数入力を自然に操作可能
  - 文字混在や指数表記を別の有効値へ変換せず、サーバー側で入力エラーとして扱う
  - 入力エラー時も入力値と変更理由を保持し、修正して再送信しやすくした
  - 整数欄と小数欄でモバイルキーボード指定を適切に切り替え
- レシート編集の保存前プレビューと保存結果の整合性を改善
  - サービス料・クーポンなどの調整種別を変更した際、明示した種別を維持して合計と支払差額を再計算
  - 指数表記や文字混在の無効な数値を別の金額として表示せず、修正後にプレビューを復帰
  - 明細・調整・支払い行を削除した直後から、削除予定行を除外して合計を更新
- 通常の編集フォームからサーバー判定専用の明細確認状態を送信しないよう整理
  - クライアント改ざんから確認状態を保護する契約を維持しつつ、正常保存時の不要なparameter警告を抑制
- 解析中カードのオフライン時再同期を改善
  - オフライン中は通信とconsole警告の反復を止め、オンライン復帰時に即時同期
  - 終端停止、通信エラー時のbackoff、古い状態への逆戻り防止は維持
- システム設定の管理操作を改善
  - DB overrideを明示的に削除する「既定値に戻す」操作を追加
  - high-risk設定のpasskey再認証、理由、確認、依存validation、監査ログをreset時も維持
  - 設定値の参照元を「管理者設定 / アプリ既定値」として表示
  - timeoutやretryの依存エラーで、先に変更すべき設定と必要値を具体的に表示
- 管理画面の解析run同期を安定化
  - 終端到達時に詳細・snapshot・retry可否を含む画面全体を更新してpollingを停止
  - 429 / 5xx / network error時はbackoffし、認証切れ・権限喪失・HTML応答時は再試行を停止

## Test/Spec

- review reason許可コード同期spec
- OCR/AI障害時のstatus契約spec
- Amount Engine review boundary spec
- OCR parser境界fixture/spec
- review確認リンクhelper/view/system/request spec
- Service Layer boundary spec
- AI deterministic guard spec
- sanitizer boundary spec
- client tampering request spec
- Amount Engine refactor guardrail spec
- Candidate family / pipeline / evidence / review facts / profile resolver spec
- generated receipts `g001`〜`g110` validator継続
- generated receipts `g111`〜`g112`を追加
  - 税額0円へ丸められる小額税率グループ
  - 実レシート由来の袋商品と数量表現の所有権回帰
- 税抜/税込/非課税30通りmatrix spec
- SecurityEventのnested `id` 誤検知防止spec
- ダウンロードリンクのTurbo無効化spec
- 店舗名・購入日時欠損レシートの匿名化fixtureを追加
  - 店舗名が写っていない場合に帳票見出しや商品名を店舗名として採用しないことを固定
  - 購入日時が取得できない場合に `purchased_at_missing` が残ることを固定
  - 税率表記をsubtotal候補として誤採用しないことを固定
- edit_save / manual contextの金額回帰specを追加
  - 部分PATCH、重複ID、更新削除競合、古い税詳細、支払・調整のみの変更、欠損理由、割引再計算を固定
  - generated comparatorでunit price、quantity、line total、tax rate、discount amountの差分をFAIL扱いに強化
- strict numeric input、ReceiptInput関係検証、負数合計、単一税率調整、明示調整種別、項目別review解除の回帰specを追加
- generated comparatorで危険方向のstatus差をFAIL扱いに強化
- 複数タブ編集の楽観ロックrequest/model specを追加
- 解析runの重複stage、finalize原子性、enqueue補償、runtime config固定、error秘匿の回帰specを追加
- 解析失敗とUsage limit超過の片側保存失敗を再現するfault-injection specを追加
- cleanup job、storage cleanup、失敗payloadの秘匿回帰specを追加
- 外部サービス状態と依存SystemSettingsの並行更新specを追加
- OCR画像取得とAI provider呼び出し中の最大処理時間specを追加
- retry所有権、管理画面polling認証切れ、Service Layer子実装検出の回帰specを追加
- 共通数値入力の途中値保持、stepper境界、不正文字列非変換のJavaScript回帰specを追加
- システム設定の入力保持、既定値reset、依存関係、参照元表示、数値入力modeの回帰specを追加
- 管理画面の解析run終端更新と429 / 5xx backoffのJavaScript回帰specを追加
- レシート編集の調整種別変更、無効数値プレビュー、削除直後の再計算、明細確認状態の改ざん防止specを追加
- iCloud / File Providerの競合コピーをrelease前に検出するworkspace preflightを追加
  - RSpecの開始時・終了時、CI、generated receipts validator / comparatorで重複を明示して停止
  - Git管理対象とbyte単位で一致する未追跡コピーだけをdry-run確認後に削除できる安全cleanupを追加
- WebAuthn再認証済みの管理画面でhigh-risk設定の更新・既定値復帰・依存エラー時の入力保持を手動確認
  - 理由・確認操作と成功/失敗AuditLogを照合
  - 検証用に変更した設定は確認後に既定値へ戻し、既存の管理者設定を維持

直近結果:

- `bundle exec rspec`: 4128 examples, 0 failures
- `ruby bin/generated_receipts_validate`: 112 cases passed
- `bin/rubocop`: no offenses
- `npm run lint:js`: no errors
- `bundle exec rails zeitwerk:check`: All is good
