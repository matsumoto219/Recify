<% content_for :title, @legal_file_document.title %>
<% content_for :meta_description, @legal_file_document.meta_description %>

<%= render "document",
      legal_document: @legal_document,
      file_document: @legal_file_document %>
