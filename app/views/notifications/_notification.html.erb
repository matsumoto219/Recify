<%
  unread = notification.unread?
  item_classes = [
    "token-bg-card",
    "token-border-soft",
    "rounded-lg border p-4 transition-colors",
    ("token-brand-soft-bg" if unread)
  ].compact.join(" ")
%>

<article id="<%= dom_id(notification) %>" class="<%= item_classes %>">
  <div class="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between">
    <div class="min-w-0 flex-1">
      <div class="flex items-center gap-2">
        <% if unread %>
          <span class="h-2 w-2 rounded-full token-state-error-soft" aria-hidden="true"></span>
        <% end %>
        <h2 class="truncate text-sm font-bold token-text-base"><%= notification.title %></h2>
      </div>

      <% if notification.body.present? %>
        <p class="mt-2 text-sm token-text-muted"><%= notification.body %></p>
      <% end %>

      <p class="mt-3 text-xs token-text-subtle">
        <%= l(notification.created_at, format: :short) %>
      </p>
    </div>

    <div class="flex shrink-0 items-center gap-2">
      <% if unread %>
        <%= button_to t("notifications.item.mark_as_read"),
          read_notification_path(notification),
          method: :patch,
          class: "inline-flex items-center justify-center rounded-lg token-border-soft token-bg-card token-hover-bg-card-subtle px-3 py-2 text-xs font-semibold token-text-base transition-colors cursor-pointer" %>
      <% elsif notification.action_path.present? %>
        <%= link_to t("notifications.item.open"),
          notification.action_path,
          class: "inline-flex items-center justify-center rounded-lg token-border-soft token-bg-card token-hover-bg-card-subtle px-3 py-2 text-xs font-semibold token-text-base transition-colors" %>
      <% end %>
    </div>
  </div>
</article>
