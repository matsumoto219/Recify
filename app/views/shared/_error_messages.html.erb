<% if obj.errors.any? %>
  <div class="error-messages">
    <h2><%= "#{obj.errors.count}件のエラーがあります" %></h2>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
