<div class="min-h-screen flex items-center justify-center p-6 md:p-8 relative overflow-hidden">
  <%= render "shared/ui/layout/ambient_background" %>

  <div class="w-full max-w-[440px] z-10 space-y-8">
    <section aria-labelledby="unlock-resend-page-title">
      <%= render "shared/ui/miscellaneous/icon" %>
      <%= render "shared/ui/layout/section_header",
            title: t("auth.unlocks.new.title"),
            subtitle: t("auth.unlocks.new.subtitle"),
            title_id: "unlock-resend-page-title",
            align: :center,
            title_size: "text-3xl",
            subtitle_size: "text-sm",
            spacing_class: "space-y-3" %>
    </section>

    <%= render "shared/ui/layout/surface_card", title_id: "unlock-resend-form-title" do %>
      <div class="px-6 md:px-10 pt-6 md:pt-10 pb-6 md:pb-10 space-y-6">
        <h2 id="unlock-resend-form-title" class="sr-only"><%= t("auth.unlocks.new.form_title") %></h2>

        <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "space-y-6" }) do |f| %>
          <%= render "shared/ui/form/text_field",
                f: f,
                attribute: :email,
                label: t("auth.unlocks.new.fields.email"),
                icon: "mail",
                field_type: :email_field,
                placeholder: t("auth.unlocks.new.fields.email_placeholder"),
                autocomplete: "email",
                autofocus: true,
                required: true,
                size: :prominent,
                label_class: "text-xs font-bold uppercase tracking-widest block px-1",
                error_message_class: "px-1 text-xs leading-relaxed" %>

          <%= render "shared/ui/actions/button",
                label: t("auth.unlocks.new.buttons.submit"),
                type: "submit",
                variant: :primary,
                size: :lg,
                weight: :bold,
                radius: :lg,
                full_width: true,
                disable_with: t("auth.unlocks.new.buttons.submitting") %>
        <% end %>

        <%= render "shared/ui/layout/section_divider", variant: :gradient, class: "mt-10 mb-10" %>

        <%= render "shared/ui/navigation/navigation_link",
              path: new_session_path(resource_name),
              text: t("auth.unlocks.new.back_to_login"),
              arrow: :left %>
      </div>
    <% end %>
  </div>
</div>
